<?php

/* Strings visíveis publicamente em páginas de erro. */
const DATABASE_ERROR = 'Erro na base de dados.';

const INVALID_CLIENT_ID_ERROR = 'Usuário não é válido.';

const INVALID_NAME_ERROR = 'Nome não é válido.';

const INVALID_EMAIL_ERROR = 'Email não é válido.';

const INVALID_CPF_ERROR = 'Pseudo-CPF não é válido.';

const INVALID_PHONE_ERROR = 'Pseudo-telefone não é válido.';

const NO_CLIENT_ERROR = 'Usuário não existe.';

const INTERNAL_ERROR = 'Erro interno.';
?>
